<Window x:Class="RamProfilePlacer.Addin.UI.ProfileSelectDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Выбор профиля"
        Width="480" Height="340"
        WindowStartupLocation="CenterOwner"
        ResizeMode="NoResize"
        Background="#F5F5F5">

    <Window.Resources>
        <Style TargetType="Button">
            <Setter Property="Padding" Value="16,6"/>
            <Setter Property="MinWidth" Value="80"/>
            <Setter Property="Margin" Value="4"/>
        </Style>
        <Style TargetType="TextBlock" x:Key="LabelStyle">
            <Setter Property="Margin" Value="0,0,0,4"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Foreground" Value="#333"/>
        </Style>
    </Window.Resources>

    <Grid Margin="16">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Заголовок -->
        <TextBlock Grid.Row="0"
                   Text="Размещение профилей по периметру проёмов"
                   FontSize="14" FontWeight="Bold"
                   Foreground="#1A4E8C"
                   Margin="0,0,0,12"/>

        <!-- Метка списка -->
        <TextBlock Grid.Row="1"
                   Style="{StaticResource LabelStyle}"
                   Text="Семейство профиля (Обобщённые модели, WorkPlaneBased):"/>

        <!-- Список семейств -->
        <ListBox Grid.Row="2"
                 x:Name="FamilyListBox"
                 Margin="0,0,0,12"
                 BorderThickness="1"
                 BorderBrush="#CCC"
                 DisplayMemberPath="Name"
                 SelectionChanged="FamilyListBox_SelectionChanged">
            <ListBox.ItemContainerStyle>
                <Style TargetType="ListBoxItem">
                    <Setter Property="Padding" Value="8,4"/>
                </Style>
            </ListBox.ItemContainerStyle>
        </ListBox>

        <!-- Кнопки -->
        <StackPanel Grid.Row="3"
                    Orientation="Horizontal"
                    HorizontalAlignment="Right">
            <Button x:Name="OkButton"
                    Content="ОК"
                    IsDefault="True"
                    IsEnabled="False"
                    Click="OkButton_Click"/>
            <Button Content="Отмена"
                    IsCancel="True"
                    Click="CancelButton_Click"/>
        </StackPanel>
    </Grid>
</Window>
